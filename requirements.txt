# first you need to install torch with cuda 12.x
# pip install torch==2.9.0 --index-url https://download.pytorch.org/whl/cu126

absl-py>=1.4.0
accelerate>=1.8.1
datasets>=3.6.0
matplotlib
numpy
pandas
peft
scikit-learn
scipy
tensorflow>=2.5.0rc0
tqdm
# For the models in the original paper we use a specific version of transformers
# increase the version number to use the latest models.
transformers
more-itertools
jupyterlab